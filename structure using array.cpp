#include<stdio.h>
//#include<string.h>
struct date
{
	int day;
	int month;
}p,q;
struct class
{
	int ix;
	int x;
}a,b;
//int school(struct student d);
int main()
{
       p.day=01;
       p.month=03;
       q.day=04;
       //q.month=08;
       a.ix=38'
	   a.x=87;
	   b.ix=86;
	   b.x=101; 
	   cout ("%d-%d\n",p.day, p.month);
	    cout("%d-%d-%d\n",q.day, q.month) ;
	     cout ("%d-%d\n",a.ix,a.x);
	    cout("%d-%d-%d\n",b.ix,b.x) ;
		     
	return 0;
}
